name: Build and Deploy to DockerHub and EC2

on:
  push:
    branches:
      - main
    paths:
      - frontend/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repository
        uses: actions/checkout@v3

      # Login to DockerHub
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # Build and Push Docker Image to DockerHub
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: eviltwin7648/eventapp-frontend:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GCP Compute Engine
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.GCP_SSH_HOST }}
          username: ${{ secrets.GCP_SSH_USERNAME }}
          key: ${{ secrets.GCP_SSH_KEY }}
          script: |
            sudo docker pull eviltwin7648/eventapp-frontend:latest
            sudo docker stop eventapp-frontend || true
            sudo docker rm eventapp-frontend || true
            sudo docker run -e DATABASE_URL=${{ secrets.DATABASE_URL }} -d --name eventapp-frontend -p 5000:5000 eviltwin7648/eventapp-frontend:latest
